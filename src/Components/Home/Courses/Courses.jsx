import SectionTitle from "../../../Shared/SectionTitle/SectionTitle";

const Courses = () => {
    return (
        <div className="container mx-auto h-screen">
            <SectionTitle title="Our Most Popular Courses" subTitle="Explore Programs" description="Let's join our famous class, the knowledge provided will definitely be useful for you."></SectionTitle>
        </div>
    );
};

export default Courses;